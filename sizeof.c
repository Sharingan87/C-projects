#include <stdio.h>
#include <limits.h>

int main(int argc, char const *argv[]) {
  printf("The size of a float is: %d\n", sizeof(float));
  printf("The size of a char is: %d\n", sizeof(char));
  return 0;
}
